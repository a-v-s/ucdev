        
stm32f1xx_hal_i2c.c:1291

		/* Two bytes */
        else if (hi2c->XferSize == 2U)
        {
          /* Wait until BTF flag is set */
          if (I2C_WaitOnFlagUntilTimeout(hi2c, I2C_FLAG_BTF, RESET, Timeout, tickstart) != HAL_OK)
          {
            return HAL_ERROR;
          }


--- geretates error, while reading, SCL remains low 
--- when identifying hcd1080_identify()

reading:
	lm75b
	bh1750
	hcd1080
	ccs811

initialising:
	
